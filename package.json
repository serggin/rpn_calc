{
  "name": "rpn_calc",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "tsc": "tsc",
    "watch": "tsc --watch",
    "serve": "tsc && browser-sync start --server 'html' --watch --s 'html/dist'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.14",
    "typescript": "^4.2.3"
  }
}
